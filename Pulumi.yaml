name: api.tommybradbury.co.uk-infrastructure
description: Deploys the Bref PHP Lambda API via API Gateway and custom domain.
runtime:
  name: nodejs
  options:
    packagemanager: npm
config:
  pulumi:tags:
    value:
      pulumi:template: typescript
